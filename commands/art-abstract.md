---
description: art-paper art-paper `abstract-only` mode — abstract + keywords
model: sonnet
---

Trigger the `art-paper` skill in `abstract-only` mode. Produces an English abstract plus keywords (art-paper v0.1 default — the bilingual-abstract machinery is inherited from ARS but is not the v0.1 default). Fidelity spectrum, medium oversight. Carries the v3.6.7 `report_compiler_agent` PATTERN PROTECTION layer when invoked through the pipeline.

Mode reference: `MODE_REGISTRY.md` § art-paper.
Skill entry: `art-paper/SKILL.md`.
