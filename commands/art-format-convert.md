---
description: art-paper art-paper `format-convert` mode — convert to LaTeX / DOCX / PDF / Markdown
model: sonnet
---

Trigger the `art-paper` skill in `format-convert` mode. Converts a paper between LaTeX, DOCX (via Pandoc), PDF, or Markdown, and converts citation styles between major formats. Fidelity spectrum, low oversight.

Mode reference: `MODE_REGISTRY.md` § art-paper.
Skill entry: `art-paper/SKILL.md`.
